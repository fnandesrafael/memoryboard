import Root from './fragments/Root';
import Import from './fragments/Importer';
import Save from './fragments/Save';
import Lit from './fragments/Lit';

export const Actions = {
  Root,
  Import,
  Save,
  Lit,
};

export default Actions;
